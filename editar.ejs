<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Producto</title>
  <link href="/public/tailwind.css" rel="stylesheet">
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      document.getElementById('add-talla').addEventListener('click', () => {
        const container = document.getElementById('tallas-container');
        const div = document.createElement('div');
        div.className = 'flex mb-4';
        div.innerHTML = `
          <input type="text" name="numeros[]" placeholder="Número" class="w-1/2 px-3 py-2 border rounded focus:outline-none focus:border-green-600 mr-2">
          <input type="number" name="cantidades[]" placeholder="Cantidad" class="w-1/2 px-3 py-2 border rounded focus:outline-none focus:border-green-600">
          <button type="button" class="text-red-500 ml-2 remove-talla">Eliminar</button>
        `;
        container.appendChild(div);
      });

      document.getElementById('tallas-container').addEventListener('click', (e) => {
        if (e.target.classList.contains('remove-talla')) {
          e.target.parentElement.remove();
        }
      });
    });
  </script>
</head>
<body class="bg-gray-100 text-gray-900">
  <header class="bg-green-600 text-white text-center py-4">
    <h1 class="text-3xl">Zapatería</h1>
  </header>
  <div class="container mx-auto p-4 flex justify-center items-center h-screen">
    <div class="w-full max-w-lg bg-white p-8 rounded shadow-md">
      <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">Editar Producto</h2>
      <form action="/api/producto/<%= producto.ProductoId %>?_method=PUT" method="POST">
        <div class="mb-4">
          <label class="block text-sm font-medium mb-2 text-gray-700">Modelo:</label>
          <input type="text" name="modelo" value="<%= producto.Modelo %>" required class="w-full px-3 py-2 border rounded focus:outline-none focus:border-green-600">
        </div>
        <div class="mb-4">
          <label class="block text-sm font-medium mb-2 text-gray-700">Color:</label>
          <input type="text" name="color" value="<%= producto.Color %>" required class="w-full px-3 py-2 border rounded focus:outline-none focus:border-green-600">
        </div>
        <div class="mb-4">
          <label class="block text-sm font-medium mb-2 text-gray-700">Precio:</label>
          <input type="number" step="0.01" name="precio" value="<%= producto.Precio %>" required class="w-full px-3 py-2 border rounded focus:outline-none focus:border-green-600">
        </div>
        <div class="mb-4">
          <label class="block text-sm font-medium mb-2 text-gray-700">Categoría:</label>
          <div class="flex flex-wrap">
            <% categorias.forEach(categoria => { %>
              <label class="inline-flex items-center m-2">
                <input type="radio" name="categoriaId" value="<%= categoria.CategoriaId %>" <%= producto.CategoriaId === categoria.CategoriaId ? 'checked' : '' %> class="form-radio h-4 w-4 text-green-600">
                <span class="ml-2 text-gray-700"><%= categoria.Genero %></span>
              </label>
            <% }) %>
          </div>
        </div>
        <div id="tallas-container" class="mb-4">
          <label class="block text-sm font-medium mb-2 text-gray-700">Tallas y Cantidades:</label>
          <% if (tallas && tallas.length > 0) { %>
            <% tallas.forEach(talla => { %>
              <div class="flex mb-4">
                <input type="text" name="numeros[]" value="<%= talla.Numero %>" placeholder="Número" class="w-1/2 px-3 py-2 border rounded focus:outline-none focus:border-green-600 mr-2">
                <input type="number" name="cantidades[]" value="<%= talla.Cantidad %>" placeholder="Cantidad" class="w-1/2 px-3 py-2 border rounded focus:outline-none focus:border-green-600">
                <button type="button" class="text-red-500 ml-2 remove-talla">Eliminar</button>
              </div>
            <% }) %>
          <% } %>
        </div>
        <button type="button" id="add-talla" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 mb-4">Agregar Talla</button>
        <button type="submit" class="w-full bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">Actualizar</button>
      </form>
    </div>
  </div>
  <footer class="bg-green-600 text-white text-center py-4">
    <p class="text-sm">Página creada por alumnos de la Universidad Politécnica de Pachuca</p>
  </footer>
</body>
</html>
